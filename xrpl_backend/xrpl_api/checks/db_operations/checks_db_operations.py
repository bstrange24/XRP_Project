import json
import logging

import django

logger = logging.getLogger('xrpl_app')

# Function to save escrow response (not a model method, but included for convenience)
def save_checks_create_response(response):
    pass

